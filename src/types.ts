export enum SIZE_TYPE {
    large = 'large',
    medium = 'medium',
    small = 'small'
}

export enum VARIANT {
    primary = 'primary',
    secondery = 'secondary'
}
export interface DataChart {
    name: string;
    value: string | number;
  }